<UserControl x:Class="NavigationService.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NavigationService"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Border BorderThickness="1" CornerRadius="5">
        <StackPanel>
            <TextBlock Text="UserName"/>
            <TextBox Width="100" Height="20" Text="{Binding UserName}"/>
            <TextBlock Text="Password"/>
            <TextBox Width="100" Height="20"/>
            <Button Content="Login" Width="100" Height="20" Command="{Binding NaviToHomeCommand}"/>
        </StackPanel>
    </Border>

</UserControl>
